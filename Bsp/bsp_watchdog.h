#ifndef __BSP_WDG_H
#define __BSP_WDG_H

#include <stdint.h>

/* =======================================================
 * API：独立看门狗初始化
 * 参数：timeout_ms - 超时复位时间 (单位：毫秒 ms)
 * (STM32F4 最大支持约 8000ms 的超时时间)
 * ======================================================= */
void bsp_wdg_init(uint16_t timeout_ms);

/* =======================================================
 * API：喂狗 (重载计数器)
 * ======================================================= */
void bsp_wdg_feed(void);

#endif // __BSP_WDG_H